DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"
JWT_SECRET="replace-with-a-long-random-string"
PORT=4000

# Optional — if unset, OTPs are logged to the console instead of sent via SMS.
MSG91_AUTH_KEY=""
MSG91_TEMPLATE_ID=""
MSG91_SENDER_ID=""
